{
  "name": "ai-content-repurposing",
  "private": true,
  "version": "1.0.0",
  "description": "AI Content Repurposing Engine — three-tier monorepo (frontend / backend / database)",
  "workspaces": [
    "frontend",
    "backend",
    "database"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace frontend",
    "dev:backend": "npm run dev --workspace backend",
    "build:frontend": "npm run build --workspace frontend",
    "build:backend": "npm run build --workspace backend",
    "build:database": "npm run build --workspace database",
    "build": "npm run build:database && npm run build:backend && npm run build:frontend",
    "typecheck": "npm run typecheck --workspace database && npm run typecheck --workspace backend",
    "db:generate": "npm run db:generate --workspace database",
    "db:migrate": "npm run db:migrate --workspace database",
    "db:push": "npm run db:push --workspace database"
  }
}
