apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: nova
  labels:
    app.kubernetes.io/name: frontend
    app.kubernetes.io/part-of: nova
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  type: LoadBalancer
